{
  "language": "en",
  "text": "Telegram Bot Integration – Implementation Plan\n\n## Phase 0: Foundation\n\n### Task 0.1 – Add `@chatbot/telegram` path alias to root tsconfig\n\nFile: `tsconfig.base.json`\n\n```json\n\"@chatbot/telegram\": [\"libs/telegram/src/index.ts\"],\n\"@chatbot/telegram/*\": [\"libs/telegram/src/*\"]\n```\n\n### Task 0.2 – Register `libs/telegram` in Nx workspace\n\nFile: `nx.json` → `projects` array\n\n```json\n\"libs/telegram\": {\"tags\": [\"scope:telegram\", \"type:lib\"]}\n```\n\n### Task 0.3 – Prisma schema changes\n\nFile: `prisma/schema.prisma`\n\nAdd 5 models after WhatsApp models (around line 811):\n\n- `TelegramAccount` – mirrors `WhatsAppAccount`\n  - Fields: `id`, `tenantId`, `botToken` (encrypted), `botName`, `botUsername`, `webhookSecret`, `isActive`, `createdAt`, `updatedAt`\n  - Map: `@@map(\"telegram_accounts\")`\n\n- `TelegramRouting` – mirrors `WhatsAppRouting`\n  - Fields: `id`, `tenantId`, `accountId`, `graphId`, `name`, `isActive`, `createdAt`, `updatedAt`\n  - Map: `@@map(\"telegram_routing\")`\n\n- `TelegramRoutingRule` – mirrors `WhatsAppRoutingRule`\n  - Fields: `id`, `routingId`, `condition`, `graphId`, `priority`, `createdAt`\n  - Map: `@@map(\"telegram_routing_rules\")`\n\n- `TelegramSession` – mirrors `WhatsAppSession` but WITHOUT 24h window fields\n  - Fields: `id`, `tenantId`, `accountId`, `chatId`, `state`, `lastGraphId`, `context`, `createdAt`, `updatedAt`\n  - Map: `@@map(\"telegram_sessions\")`\n\n- `TelegramMessage` – mirrors `WhatsAppMessage`\n  - Fields: `id`, `tenantId`, `accountId`, `telegramMessageId`, `chatId`, `fromId`, `direction` (enum IN/OUT), `type` (text, photo, etc.), `content`, `rawPayload`, `graphId`, `createdAt`\n  - Map: `@@map(\"telegram_messages\")`\n\n- Add `telegramAccounts TelegramAccount[]` to `Tenant` model\n\nRun: `bunx prisma generate --schema=./prisma/schema.prisma`\n\n## Phase 1: Library – `libs/telegram/`\n\n### Task 1.1 – Create library shell\n\nFiles to create:\n- `libs/telegram/package.json` – deps: `@chatbot/shared`, `grammy`, `zod`, `@t3-oss/env-core`\n- `libs/telegram/tsconfig.json` – extends base, includes `@chatbot/shared` path\n- `libs/telegram/vitest.config.ts` – test config with path aliases\n- `libs/telegram/src/index.ts` – public exports\n\n### Task 1.2 – Environment config\n\nFile: `libs/telegram/src/config/env.ts`\n\nZod schema for:\n- `TELEGRAM_API_BASE` (default: `https://api.telegram.org`)\n- `ENCRYPTION_KEY` (reused from shared)\n\n### Task 1.3 – Telegram Bot API client\n\nFile: `libs/telegram/src/client/bot-api.ts`\n\nPattern: same as `libs/whatsapp/src/client/meta-api.ts` but using `grammy`\n\nClass `TelegramBotApi` with methods:\n- `setWebhook(url, secretToken)` → raw HTTP to `setWebhook`\n- `deleteWebhook()` → raw HTTP\n- `getMe()` → raw HTTP\n- `sendMessage(chatId, text, options)` → uses `grammy` types for options (parse_mode, reply_markup, etc.)\n- `sendPhoto(chatId, photoUrl, caption, options)` → uses `grammy` types\n- `answerCallbackQuery(callbackQueryId, text)` → uses `grammy` types\n- `editMessageReplyMarkup(chatId, messageId, markup)` → uses `grammy` types\n\nUses `logger` from `@chatbot/shared` for all calls.\n\n### Task 1.4 – Webhook parser\n\nFile: `libs/telegram/src/webhook/parser.ts`\n\nPattern: same as `libs/whatsapp/src/webhook/parser.ts`\n\nFunction `parseWebhookBody(body): TelegramWebhookEvent` that handles:\n- `message` (text, photo, document, etc.)\n- `callback_query` (inline button click)\n- `edited_message`\n- `channel_post`\n\nExtracts: `updateId`, `type`, `chatId`, `fromId`, `fromName`, `text`, `photoUrls`, `callbackData`, `callbackQueryId`, `isGroup`, `rawUpdate`\n\n### Task 1.5 – Webhook signature validator\n\nFile: `libs/telegram/src/webhook/signature.ts`\n\nPattern: same as `libs/whatsapp/src/webhook/signature.ts`\n\nFunction `validateWebhookSecret(headerSecret, configuredSecret)` → compares `crypto.timingSafeEqual`\n\n### Task 1.6 – Session manager\n\nFile: `libs/telegram/src/session/session-manager.ts`\n\nPattern: mirrors `libs/whatsapp/src/session/session-manager.ts` but WITHOUT 24h window logic\n\nClass `TelegramSessionManager` with methods:\n- `getOrCreateSession(tenantId, accountId, chatId)` – upsert, return session\n- `updateSession(sessionId, state, context, lastGraphId)`\n- `resetSession(tenantId, accountId, chatId)` – clear state/context\n- `getSession(tenantId, accountId, chatId)`\n\n### Task 1.7 – Command handler\n\nFile: `libs/telegram/src/session/command-handler.ts`\n\nPattern: mirrors `libs/whatsapp/src/session/command-handler.ts`\n\nFunction `detectCommand(text): { isCommand, command, args }`\n\nHandles: `/start`, `/reset`, `/help`\n- `/start` → triggers welcome graph or default routing\n- `/reset` → clears session + replies \"Conversation reset\"\n- `/help` → replies with built-in help text (or routes to help graph if configured)\n\n### Task 1.8 – Router factory + keyword router\n\nFiles:\n- `libs/telegram/src/router/factory.ts` – `createRouter()` returning `KeywordRouter`\n- `libs/telegram/src/router/keyword-router.ts` – same strategy as WhatsApp\n\nClass `KeywordRouter` with method:\n- `route(tenantId, accountId, text, session): { graphId, routingId }`\n\nRules evaluated by priority. Falls back to default routing if no match.\n\n### Task 1.9 – Message processor (orchestrator)\n\nFile: `libs/telegram/src/processor/message-processor.ts`\n\nPattern: exact mirror of `libs/whatsapp/src/processor/message-processor.ts`\n\nClass `TelegramMessageProcessor` orchestration flow:\n1. Parse webhook → `TelegramWebhookEvent`\n2. Deduplicate via `telegramMessageId` + `chatId` (check `TelegramMessage` table)\n3. Acquire contact lock (using same Redis/concurrency infra as WhatsApp, key: `telegram:${tenantId}:${chatId}`)\n4. Store incoming message in `TelegramMessage` (direction: IN)\n5. Detect commands → handle `/reset`, `/help`, `/start`\n6. Get or create session\n7. Route to graph (via keyword router or default routing)\n8. Execute graph via `TelegramAgentExecutor`\n9. Send response(s) via Bot API\n10. Store outgoing message(s) in `TelegramMessage` (direction: OUT)\n11. Release contact lock\n\nGroup chat logic (inserted at step 5):\n- If `isGroup` and message is not a /command and bot is not @mentioned → skip (no-op, return 200)\n\n### Task 1.10 – Agent executor (graph bridge)\n\nFile: `libs/telegram/src/processor/agent-executor.ts`\n\nPattern: exact mirror of `libs/whatsapp/src/processor/agent-executor.ts`\n\nClass `TelegramAgentExecutor` with method:\n- `execute(tenantId, accountId, chatId, text, session, photoUrls?, callbackData?): Promise<AgentResponse>`\n\nInjects channels into `GraphExecutor.executeFromState()`:\n```\ntg_chat_id: chatId\ntg_account_id: accountId\ntg_text: text\ntg_photo_urls: photoUrls\ntg_callback_data: callbackData\ntg_from_name: fromName\ntg_is_group: isGroup\ntg_message_id: telegramMessageId\n```\n\nCallback flow:\n- If `callbackData` present → re-run graph with `tg_callback_data` populated\n- After graph execution → call `answerCallbackQuery()` if `callbackQueryId` exists\n\n### Task 1.11 – Factory\n\nFile: `libs/telegram/src/factory.ts`\n\nPattern: same as `libs/whatsapp/src/factory.ts`\n\nFunction `createMessageProcessor()` that wires all dependencies:\n- Prisma client from `@chatbot/shared`\n- `TelegramBotApi` client\n- `TelegramSessionManager`\n- `KeywordRouter`\n- `TelegramAgentExecutor`\n- Logger\n\n### Task 1.12 – Library exports\n\nFile: `libs/telegram/src/index.ts`\n\nExport:\n- `TelegramMessageProcessor`\n- `TelegramBotApi`\n- `createMessageProcessor`\n- `parseWebhookBody`\n- `validateWebhookSecret`\n- `TelegramSessionManager`\n- types from `types.ts`\n\n## Phase 2: Agent Studio – Graph Nodes\n\n### Task 2.1 – Node type definitions\n\nFile: `libs/agent-studio/src/types/nodes.ts`\n\nAdd 3 new interfaces after WhatsApp ones:\n\n```typescript\nexport interface TelegramTriggerNodeConfig extends BaseNodeConfig {\n  channelMap: Record<string, string>;\n  // e.g. map tg_chat_id → chat_id channel, tg_text → user_input channel\n}\n\nexport interface TelegramSendNodeConfig extends BaseNodeConfig {\n  accountId: string;   // which Telegram bot account to use\n  text: string;        // message text (supports {{variable}} interpolation)\n  parseMode?: 'Markdown' | 'HTML';\n  replyToMessageId?: string;\n}\n\nexport interface TelegramSendButtonsNodeConfig extends BaseNodeConfig {\n  accountId: string;\n  text: string;\n  buttons: { text: string; callbackData: string }[];\n  parseMode?: 'Markdown' | 'HTML';\n  buttonsChannel?: string;  // channel name to read dynamic buttons from\n}\n```\n\nUpdate `NodeType` union and `NodeConfig` union to include the 3 new types.\n\n### Task 2.2 – Zod schemas\n\nFiles:\n- `libs/agent-studio/src/registry/schemas/telegram-trigger.ts`\n- `libs/agent-studio/src/registry/schemas/telegram-send.ts`\n- `libs/agent-studio/src/registry/schemas/telegram-send-buttons.ts`\n\nPattern: exact copy of WhatsApp schema files but with Telegram fields.\n\nEach exports `schema` (Zod object) and `type` (TypeScript inferred type).\n\n### Task 2.3 – Node registry update\n\nFile: `libs/agent-studio/src/registry/node-registry.ts`\n\nAdd 3 new `NodeDefinition` entries to the `nodeDefinitions` array:\n\n1. `telegram_trigger` – category: TRIGGER, label: \"Telegram Trigger\", icon: \"MessageSquare\"\n2. `telegram_send` – category: ACTION, label: \"Telegram Send Message\", icon: \"Send\"\n3. `telegram_send_buttons` – category: ACTION, label: \"Telegram Send Buttons\", icon: \"MousePointerClick\"\n\nAdd 3 new schema arms to `nodeConfigSchema` discriminated union.\n\n### Task 2.4 – Node executors\n\nFiles:\n- `libs/agent-studio/src/execution/node-executors/telegram-trigger-executor.ts`\n- `libs/agent-studio/src/execution/node-executors/telegram-send-executor.ts`\n- `libs/agent-studio/src/execution/node-executors/telegram-send-buttons-executor.ts`\n\nPattern: exact mirrors of WhatsApp executor files.\n\n**TelegramTriggerExecutor:**\n- Reads `tg_chat_id`, `tg_text`, `tg_photo_urls`, `tg_callback_data`, `tg_from_name`, `tg_is_group` from channels\n- Applies `channelMap` to rename channels\n- Returns `stateUpdates`\n\n**TelegramSendExecutor:**\n- Looks up `TelegramAccount` by `accountId` (from node config)\n- Decrypts `botToken` via `EncryptionService`\n- Instantiates `TelegramBotApi` with decrypted token\n- Interpolates `{{variable}}` in `text` using channel values\n- Calls `sendMessage(chatId, interpolatedText, { parseMode, replyToMessageId })`\n- Returns `tg_last_sent_message_id`\n\n**TelegramSendButtonsExecutor:**\n- Same lookup + decrypt as send executor\n- Builds `InlineKeyboardMarkup` from `buttons` array OR from `buttonsChannel` (channel precedence: `buttonsChannel` > `buttons`)\n- Calls `sendMessage(chatId, interpolatedText, { parseMode, replyMarkup })`\n- Returns `tg_last_sent_message_id`\n\n### Task 2.5 – Executor exports\n\nFile: `libs/agent-studio/src/execution/node-executors/index.ts`\n\nAdd 3 new exports:\n```typescript\nexport { TelegramTriggerExecutor } from './telegram-trigger-executor';\nexport { TelegramSendExecutor } from './telegram-send-executor';\nexport { TelegramSendButtonsExecutor } from './telegram-send-buttons-executor';\n```\n\n### Task 2.6 – Server factory update\n\nFile: `libs/agent-studio/src/server.ts`\n\nUpdate `createNodeExecutors()` to instantiate 3 new executors:\n```typescript\ntelegram_trigger: new TelegramTriggerExecutor(),\ntelegram_send: new TelegramSendExecutor(),\ntelegram_send_buttons: new TelegramSendButtonsExecutor(),\n```\n\n## Phase 3: Frontend – Canvas Config Forms\n\n### Task 3.1 – Telegram trigger node form\n\nFile: `apps/web-ui/components/agents/config/telegram-trigger-node-form.tsx`\n\nPattern: mirror of `whatsapp-trigger-node-form.tsx`\n\nUses `@tanstack/react-form` + Zod + shadcn/ui.\n\nFields:\n- `channelMap` – key-value pairs (e.g. `tg_chat_id` → `chat_id`)\n\n### Task 3.2 – Telegram send node form\n\nFile: `apps/web-ui/components/agents/config/telegram-send-node-form.tsx`\n\nFields:\n- `accountId` – dropdown of tenant's Telegram accounts (fetched from API)\n- `text` – textarea with variable interpolation hint\n- `parseMode` – select: None / Markdown / HTML\n- `replyToMessageId` – optional input\n\n### Task 3.3 – Telegram send buttons node form\n\nFile: `apps/web-ui/components/agents/config/telegram-send-buttons-node-form.tsx`\n\nFields:\n- `accountId` – dropdown\n- `text` – textarea\n- `buttons` – dynamic list of { text, callbackData } pairs (add/remove rows)\n- `buttonsChannel` – optional input (channel name for dynamic buttons)\n- `parseMode` – select\n\n### Task 3.4 – Form exports\n\nFile: `apps/web-ui/components/agents/config/index.ts`\n\nAdd 3 new exports.\n\n### Task 3.5 – Node palette registration\n\nFile: `apps/web-ui/components/agents/canvas/node-palette.tsx`\n\nAdd 3 new node types to the palette (after WhatsApp nodes).\n\n### Task 3.6 – Node type registration\n\nFile: `apps/web-ui/components/agents/canvas/node-types.ts`\n\nAdd 3 new entries to `nodeTypes` map.\n\n## Phase 4: Frontend – Settings Page\n\n### Task 4.1 – Telegram settings page\n\nFile: `apps/web-ui/app/(dashboard)/settings/channels/telegram/page.tsx`\n\nPattern: exact mirror of `whatsapp/page.tsx`\n\nFeatures:\n- DataTable listing tenant's `TelegramAccount`s\n- Columns: bot name, username, status (active/inactive), created at\n- \"Connect Bot\" button → Dialog with form\n- \"Disconnect\" action → AlertDialog confirmation\n- Row click → shows routing config (which graph this bot routes to)\n\n### Task 4.2 – Connect bot dialog form\n\nEmbedded in page or separate component.\n\nFields:\n- `botToken` – password input (paste from BotFather)\n- `name` – optional display name\n\nOn submit:\n1. Call `POST /api/telegram/connect` with token\n2. Backend does `getMe()` → `setWebhook()` → encrypt token → create DB record\n3. UI refreshes table\n\n### Task 4.3 – Sidebar navigation\n\nFile: `apps/web-ui/app/(dashboard)/settings/channels/layout.tsx` (or settings nav)\n\nAdd \"Telegram\" nav item under Channels section.\n\n## Phase 5: API Routes\n\n### Task 5.1 – Webhook route\n\nFile: `apps/web-ui/app/api/webhooks/telegram/[accountId]/route.ts`\n\nPattern: mirror of `apps/web-ui/app/api/webhooks/whatsapp/[accountId]/route.ts`\n\n`POST` handler:\n1. Read `X-Telegram-Bot-Api-Secret-Token` header\n2. Look up `TelegramAccount` by `accountId`\n3. `validateWebhookSecret(headerSecret, account.webhookSecret)`\n4. If invalid → 401\n5. Parse body via `parseWebhookBody()`\n6. Call `createMessageProcessor()` → `processor.process(event, account)`\n7. Return 200 (always acknowledge to Telegram)\n\n`GET` handler:\n- Return 200 with challenge (for webhook verification if needed, though Telegram doesn't require it like Meta)\n\n### Task 5.2 – Connect route\n\nFile: `apps/web-ui/app/api/telegram/connect/route.ts`\n\n`POST` handler:\n1. `getSessionTenantId()` + `authorize('telegram', 'create')`\n2. Zod validate body: `{ botToken: string, name?: string }`\n3. Instantiate `TelegramBotApi` with raw token\n4. Call `getMe()` → validate it's a real bot\n5. Call `setWebhook()` with URL: `${NEXTAUTH_URL}/api/webhooks/telegram/${newAccountId}` and generated `secretToken`\n6. If `setWebhook` fails → return 400 with error, DO NOT write to DB (fail-fast)\n7. Encrypt token via `EncryptionService`\n8. Create `TelegramAccount` record with encrypted token, bot name, username, webhook secret\n9. Return 201 with account details\n\n### Task 5.3 – Disconnect route\n\nFile: `apps/web-ui/app/api/telegram/disconnect/route.ts`\n\n`POST` handler:\n1. `getSessionTenantId()` + `authorize('telegram', 'delete')`\n2. Zod validate body: `{ accountId: string }`\n3. Look up account\n4. Call `deleteWebhook()` to clean up on Telegram side\n5. Delete `TelegramAccount` record (cascade deletes routing/rules/messages/session)\n6. Return 200\n\n### Task 5.4 – List accounts route\n\nFile: `apps/web-ui/app/api/telegram/accounts/route.ts`\n\n`GET` handler:\n1. `getSessionTenantId()` + `authorize('telegram', 'read')`\n2. Query `TelegramAccount` for tenant\n3. Return JSON array (exclude `botToken` field)\n\n### Task 5.5 – Update routing route\n\nFile: `apps/web-ui/app/api/telegram/routing/route.ts`\n\n`POST` handler:\n1. `getSessionTenantId()` + `authorize('telegram', 'update')`\n2. Zod validate body: `{ accountId, graphId, rules? }`\n3. Upsert `TelegramRouting` + `TelegramRoutingRule` records\n4. Return 200\n\n## Phase 6: Testing & Verification\n\n### Task 6.1 – Unit tests for `libs/telegram`\n\nFiles:\n- `libs/telegram/src/client/bot-api.test.ts` – mock fetch, test all API methods\n- `libs/telegram/src/webhook/parser.test.ts` – test parseWebhookBody with all update types\n- `libs/telegram/src/webhook/signature.test.ts` – test validateWebhookSecret\n- `libs/telegram/src/session/session-manager.test.ts` – test CRUD with Prisma mock\n- `libs/telegram/src/session/command-handler.test.ts` – test /start, /reset, /help detection\n- `libs/telegram/src/router/keyword-router.test.ts` – test routing logic\n- `libs/telegram/src/processor/message-processor.test.ts` – integration test with mocked deps\n- `libs/telegram/src/processor/agent-executor.test.ts` – test graph bridge\n\n### Task 6.2 – Unit tests for graph node executors\n\nFiles:\n- `libs/agent-studio/src/execution/node-executors/telegram-trigger-executor.test.ts`\n- `libs/agent-studio/src/execution/node-executors/telegram-send-executor.test.ts`\n- `libs/agent-studio/src/execution/node-executors/telegram-send-buttons-executor.test.ts`\n\n### Task 6.3 – Run existing tests\n\nCommands:\n```bash\nbun run test              # All unit tests\nbun run e2e:smoke         # Smoke tests\n```\n\nVerify no regressions in WhatsApp or other modules.\n\n### Task 6.4 – Manual verification checklist\n\n1. `bun run build` – builds all projects\n2. `bunx prisma db push` – schema applies cleanly\n3. `bun run dev` – web-ui starts\n4. Create bot with BotFather → paste token → connect → webhook set\n5. Send message to bot → webhook hits route → graph executes → reply sent\n6. Test /reset, /help, /start commands\n7. Test inline buttons → callback → graph re-executes\n8. Test group chat (only respond when @mentioned or /command)\n9. Test multiple bots per tenant\n10. Disconnect bot → webhook deleted → DB cleaned\n\n## Execution Order\n\n1. Task 0.1, 0.2, 0.3 (foundation)\n2. Task 1.1 – 1.12 (library)\n3. Task 2.1 – 2.6 (agent studio nodes)\n4. Task 3.1 – 3.6 (frontend forms)\n5. Task 4.1 – 4.3 (settings page)\n6. Task 5.1 – 5.5 (API routes)\n7. Task 6.1 – 6.4 (testing)\n\nEach task implemented by a subagent with full context of the spec + mirrored WhatsApp files.\n"}
